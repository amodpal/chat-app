{
  "name": "chat-app-clone-using-node",
  "version": "1.0.0",
  "description": "ap chat app",
  "main": "server.js",
  "scripts": {
    "build": "netlify deploy --prod",
    "nodemon": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.0",
    "socket.io": "^4.8.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}
